Atue como meu agente `template-extractor`.

Quero extrair deste projeto atual um boilerplate reutilizável.

Faça uma análise completa e me entregue:

1. arquivos e pastas que devem permanecer no núcleo do boilerplate
2. arquivos e pastas que devem virar template parametrizável
3. arquivos e pastas que devem virar módulos opcionais
4. arquivos e pastas que devem ser removidos por serem específicos do projeto atual
5. variáveis, nomes, ARNs, buckets, domínios, workflows e configs que precisam ser generalizados
6. quais placeholders precisam existir

Quero isso organizado em uma tabela conceitual:

- caminho atual
- destino no boilerplate
- tipo:
  - core
  - template
  - optional module
  - remove
- justificativa

Não implemente ainda.
Quero primeiro o mapa de extração.