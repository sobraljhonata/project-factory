"use strict";

/**
 * Placeholder inicial para SequelizeMeta e fluxo `npm run db:migrate`.
 * Substitua por DDL real (CREATE/ALTER) na primeira mudança de schema versionada em produção.
 */
module.exports = {
  async up() {},

  async down() {},
};
