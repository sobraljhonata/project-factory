# Placeholders para o módulo opcional auth-jwt (copiar linhas para o .env na raiz do projeto).
# Não commite segredos reais.

# Obrigatório com este módulo: mínimo 32 caracteres (entropia forte recomendada).
# JWT_SECRET=

# Opcionais: validação de claims
# JWT_ISSUER=
# JWT_AUDIENCE=
