/node_modules
coverage
dist
.env
.env.development
.env.test
.env.production
tsconfig.tsbuildinfo

# Terraform (estado local — nunca commitar segredos)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
**/terraform.tfvars
