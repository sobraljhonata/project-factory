const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "database", "sequelize-cli-config.cjs"),
  "migrations-path": path.resolve(__dirname, "database", "migrations"),
  "seeders-path": path.resolve(__dirname, "database", "seeders"),
};
