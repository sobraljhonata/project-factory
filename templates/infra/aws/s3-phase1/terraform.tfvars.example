# Copie para terraform.tfvars (não commitar segredos reais).

aws_region   = "us-east-1"
project_name = "{{PROJECT_SLUG}}"

# Opcional: anexa a política mínima de mídia ao usuário IAM (desenvolvimento local)
# developer_iam_user_name = "seu-usuario-iam"

# s3_public_prefix  = "public"
# enable_versioning = true
