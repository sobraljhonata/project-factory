{
  "name": "project-factory",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "build:cli": "npm run build -w @project-factory/cli",
    "create-app": "npm run build:cli && node cli/dist/cli.js",
    "test:cli": "npm run test -w @project-factory/cli",
    "smoke": "node scripts/smoke.cjs"
  }
}
